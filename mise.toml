[tools]
node = "22.14.0"

[tasks.install]
run = "npm install"

[tasks.build]
depends = ["install"]
run = "npm run build"

[tasks.dev]
run = "npm run dev"

[tasks.fmt]
run = "npm run fmt"

[tasks."deploy:prd"]
run = "npm run deploy:prd"
