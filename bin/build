#!/bin/bash

echo "Next: $(yarn next --version)"

yarn install
yarn build

# echo "hugo --gc --minify $@"
# hugo --gc --minify "$@"

# # index content into elasticsearch
# bin/define-index
# bin/index
