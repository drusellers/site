#!/bin/bash

if [ "${ES_URL}x" == "x" ]; then
  echo "sourceing"
  source .env
fi

curl -XDELETE \
  "${ES_URL}/posts"

echo ""

curl -H "Content-Type: application/json" -XPUT \
  "${ES_URL}/posts?pretty" \
  --data-binary @bin/mapping.json